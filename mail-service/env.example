# SMTP for eternalskytour.com (host and user are hardcoded in server.js)
# Copy to .env. For cPanel Node.js apps, set SMTP_PASSWORD in app environment variables.

# Required: password for info@eternalskytour.com (host, port, user are hardcoded)
SMTP_PASSWORD=your_actual_smtp_password_here

# Optional: App port (default 3000)
# PORT=3000
